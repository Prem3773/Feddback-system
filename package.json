{
  "name": "feedback-system",
  "version": "1.0.0",
  "description": "Feedback system with frontend and backend",
  "scripts": {
    "start": "concurrently \"cd Backend && npm start\" \"cd Frontend/my-app && npm start\"",
    "dev": "concurrently \"cd Backend && npm run dev\" \"cd Frontend/my-app && npm start\"",
    "install-all": "npm install && cd Backend && npm install && cd ../Frontend/my-app && npm install"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1"
  }
}
